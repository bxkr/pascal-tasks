Дан массив, заполненный случайными целыми числами в диапазоне от -30000 до 30000.

Напишите подпрограмму, отсортирует элементы массива по возрастанию количества цифр кратных трем в числе (дополнительный
массив не использовать).

Например из массива: 1 22 33 4444 533 36, получим 1 22 4444 33 533 36.

Размер массива заранее задан константой N. Используйте заголовок:

procedure sort(var m: TMas);
Тип ТMas задан как array[1..N] of longint и может использоваться для описания параметров подпрограмм.

Используйте сортировку методом простого обмена ("пузырек").

```pascal
function countThree(num: longint): longint;
begin
    countThree:=0;
    if num <> 0 then begin
      repeat
          if num mod 10 mod 3 = 0 then countThree += 1;
          num:=num div 10;
      until num div 10=0;
    end;
end;

procedure sort(var m: TMas);
var i, j, t: longint;
begin
  for i := 1 to n - 1 do
    for j := 1 to n - i do
      if countThree(m[j]) > countThree(m[j + 1]) then 
      begin
        t := m[j];
        m[j] := m[j + 1];
        m[j + 1] := t;
      end
end;
```

Код для тестов:

```pascal
procedure vvod(var m: TMas);
var
  i: longint;
begin
  for i := 1 to n do
    read(m[i]);
  readLn;
end;

procedure vivod(m: TMas);
var
  i: longint;
begin
  for i := 1 to n do
    write(m[i], ' ');
  writeLn;
end;

var
  m: TMas;
begin
  vvod(m);
  sort(m);
  vivod(m);
end.
```

| Ввод                                                                                       | Ожидаемый                                                                                  | Получено                                                                                   |
|--------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------|
| -9 -84 -11 -9 -69 15 77 7 4 95                                                             | -84 -11 15 77 7 4 -9 -9 95 -69                                                             | -84 -11 15 77 7 4 95 -9 -9 -69                                                             |
| 27708 25018 -15940 20162 25857 -14006 -5403 -6031 9622 -4970 10659 27051 -9117 -12184 7016 | 25857 -12184 27708 25018 27051 -9117 -15940 20162 -5403 9622 -4970 7016 -14006 -6031 10659 | 25857 -9117 -12184 27708 25018 9622 27051 -15940 20162 -5403 -6031 -4970 7016 -14006 10659 |
| 2 1 0                                                                                      | 2 1 0                                                                                      | 2 1 0                                                                                      |
